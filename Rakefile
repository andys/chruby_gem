require 'echoe'

Echoe.new("chruby") do |p|
  p.author = "Andrew Snow"
  p.email = 'andrew@modulus.org'
  p.summary = "Ruby gem to install chruby & ruby-install"
  p.url = "http://github.com/andys/chruby_gem"
  p.ignore_pattern = /^(pkg|doc)|(\.svn|CVS|\.bzr|\.DS|\.git)$/
end
